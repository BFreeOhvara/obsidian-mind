---
date: 2026-06-19
description: "Ohvara persistent knowledge — hard-won lessons across Supabase, React, deployment, lead scraping. Read before every session."
tags:
  - brain
  - index
---

# Memories

Persistent context and knowledge retained across sessions. Each topic lives in its own note — follow the links.

- [[LIVE_STATE]] — single current-state doc (overwritten, not appended) — read FIRST on /reload
- [[Gotchas]] — things that have bitten before and will bite again
- [[North Star]] — living goals document, read at session start
- [[Skills]] — custom slash commands and workflows

---

## Hard-Won Lessons

### Supabase / RLS

- NEVER write a policy that queries the same table it protects → infinite recursion
- Always use SECURITY DEFINER functions for profile-based lookups
- Test auth as every role before marking auth work complete
- Use `auth.uid()` directly in policies — never join through profiles

### React

- Never use HTML form tags → use onClick handlers
- All colors via CSS custom properties — no hardcoded hex in JSX
- Font weights 400 and 500 ONLY — never 600 or 700

### Deployment

- Set env vars before deploying — not after hitting a 404
- Always handle CORS headers in Supabase Edge Functions
- Return consistent error shapes: `{ error: string, code: string }`

### Lead Scraping

- Deduplicate before every insert — check phone AND business name
- Indeed MCP: `mcp.indeed.com/claude/mcp`
- Google Maps fallback for niches/markets where Indeed is thin
- Flag no-website businesses as web agency candidates

### Model Routing

- Use **Sonnet 4.6** for small fixes and routine tasks; **Fable 5** for big autonomous builds only

### Rules

- CC auto-logs every completed task to Atlas tagged `[CC | date]` — entry appended to [[Memories]], committed and pushed immediately (rule lives in [[cc-prompt-format]])
- /reload flow: old CC prints prompt → paste into new CC → new CC reads Atlas → new CC prints context summary → paste into new Claude chat → done (skill: [[reload]])

### General

- Read [[North Star]] and [[Memories]] before writing code
- Append to [[Memories]] before ending every session
- Never ask Brayden to run SQL or terminal commands manually
- **When a literal exported mockup file exists (e.g. a Claude Design `.dc.html` export), port its exact visual design literally — colors, layout, spacing, every styled element — not a reinterpretation built from the written brief alone.** Confirmed 2026-07-24: Prompt 326 built real, correct backend/data wiring but its own generic UI instead of literally porting `media/claude-design-export-ohvara-dashboard-v3.html`, and Brayden rejected the result outright — he'd spent dozens of design rounds locking down that exact look specifically so it wouldn't need to be redone. When both a written brief and a literal export exist, the export is the visual source of truth; the brief is the functional/data source of truth. Use both, don't substitute one for the other.
- **When retiring an old business's UI in favor of a new one, remove it fully — never leave old and new business nav/UI coexisting side-by-side in the same shell**, even temporarily, unless explicitly asked to keep both. Same 2026-07-24 incident: the old setter/SMB portal nav (Setter Performance, Pipeline, Payouts, Messages) shipped alongside the new insurance nav in one merged sidebar — Ohvara's pivot away from the SMB business is a full replacement, not an add-on (see [[North Star]] pivot note), so its UI needs to disappear when the new one lands, not merge with it.
- **A third-party authenticated tool's iframe embed can pass a static `curl -I` header check clean and still get blocked in the real browser.** Confirmed 2026-07-25: Prompt 328 shipped the Quoter embed after `curl -I` on `https://app.insurancetoolkits.com/fex/quoter` showed no `X-Frame-Options`/CSP frame-ancestors header, but in real logged-in use the page redirected (no shared session/cookies cross-origin) to `landing.insurancetoolkits.com`, which DOES block framing (`ERR_BLOCKED_BY_RESPONSE`). A header check on the one URL you set as `src` doesn't account for auth redirects. Verify third-party embeds logged-in in a real browser, not just a header check.

---

## Session Log

> Full entries live in per-quarter files (split out 2026-08-01 — this file had grown to ~1.6MB). Newest quarter first.

- [[2026-Q3]] — 2026-07-01 to 2026-08-01, 347 entries (includes the 2026-07-21 insurance pivot)
- [[2026-Q2]] — 2026-06-07 to 2026-06-30, 173 entries (business decisions, initial SMB-era dashboard build)

**Recent (pointer only — full write-ups in [[2026-Q3]]):**

- [CC | 2026-08-14 — Prompt 426 shipped: Atlas deep-cleaned + split into Ohvara/Restorix verticals] Ran the full investigate-then-fix pass Brayden asked for rather than guessing at "bubbles." Root causes found: ~75 blank graph nodes from `[[Prompt N]]`/bare-3-digit/`[[BREAK]]` wikilinks in prose (individual prompt numbers routinely double-bracketed even though no per-prompt note was ever meant to exist) — unwrapped all of them to plain text across `LIVE_STATE.md`, `Memories.md`, both `session-logs/` files, and the Prompt History archive. Template-placeholder stubs under `archive/template-cruft/` were still graph-indexed — excluded the relevant subfolders via Obsidian's `userIgnoreFilters` (kept `session-flow-superseded/`/`work-hubs/` visible since real historical log entries still link into those). Deleted 11 genuinely orphaned single-use files (8 `brain/claude-chrome-*.md` browser-task scripts, 3 dead pre-pivot root files referencing the old SMB rep/setter pipeline) plus 2 stray empty root files and 2 stale git worktree dirs. `LIVE_STATE.md`'s CURRENT STATE section had re-bloated to ~137KB in 2 weeks (same pattern as the 2026-08-01 split) — archived 21 closed entries to [[LIVE_STATE Prompt History (2026-06 to 2026-07)]], kept 20 current ones, shrinking the file to ~94KB (delegated the mechanical extraction to a background agent, verified its byte accounting after). Created [[ohvara_legacy_setter_pipeline_dead]] (a memory note `LIVE_STATE.md` already referenced but that never existed) and [[Patterns]] (documented in CLAUDE.md's file list, never created, one real dangling link). Archived 3 stale `status: active` mobile-audit notes + screenshots from `work/active/` to `work/archive/2026/` (a month past done, no open follow-up). Added real inbound links for 7 orphaned-but-legitimate reference files instead of deleting them (setter transcripts, closer script rewrite, brand-asset prompts) — orphaned isn't automatically junk. **Restructure:** new `restorix/` folder mirrors `brain/`'s shape for Brayden's second business (behavioral-health AI infra) — `restorix/CLAUDE.md`, [[Restorix North Star]], [[Restorix LIVE_STATE]], [[Restorix Memories]], deliberately prefixed "Restorix" (not reusing `LIVE_STATE`/`North Star` names) to avoid ambiguous-wikilink collisions with Ohvara's own files. Prompts 427 (Restorix marketing site) and 428 (Restorix setter portal MVP) moved verbatim into Restorix's own queue. Root `CLAUDE.md` gained a routing preamble so a session reads the right vertical's state before doing anything else. Final whole-vault broken-wikilink sweep came back clean (0 real broken links, verified via script after each fix). Nothing pushed to `ohvara-dashboard` or `Scraper` this session — vault-only work. Full detail in [[2026-Q3]].
- [CC | 2026-08-05 — Prompt 425 shipped: Fulfillment nav trimmed + Overview redesigned, queue empty again] `1e6f313`. Same-day correction to Prompt 424 after Brayden saw the full Closer-parity nav live — dropped My Policies/Submissions/Commissions (none apply to Fulfillment's actual job), kept Live Call/Underwriting/Quoter/My Calls/Carrier Portals/Team/Training Center/Settings/Fulfillment Queue. Performance now skips straight to Leaderboard for this role (no Production tab). AgentOverview got a real redesign, not just hidden cards — new `FulfillmentOverview` with queue-shaped stats (Open in queue, Claimed by me, Today's callbacks team-wide, Completed by me today/month) derived from the existing `useFulfillmentQueue()` set, no new schema. Build+lint clean, dev server boots clean; still no live screenshot (same standing credentials wall). Queue empty again.
- [CC | 2026-08-05 — Prompt 424 shipped: Fulfillment gets full nav shell + header bell fix, LIVE_STATE queue now empty] `422364e`. Reversed Prompt 418's scoped-down Fulfillment nav per Brayden's live feedback — `Sidebar.jsx`'s `fulfillment` entry now mirrors `closer`'s full group set (Overview/Live Call/My Policies/My Calls/Quoter/Underwriting/Submissions/Carrier Portals/Performance/Team/Training Center/Commissions) with Fulfillment Queue folded into Account; `App.jsx` widened `allowedRoles` on the 12 corresponding `/agent/*` routes. Also found and fixed a real second gap along the way: `DashboardLayout.jsx`'s `HeaderBell()` had no branch for `fulfillment` at all (fell straight to null) — routed it through the existing `CloserNotificationBell`. RLS scoping from 418 untouched — nav/route visibility only. Build+lint clean, dev server boots with zero console errors; could not get the live authenticated screenshot Brayden asked for (no saved fulfillment-role credentials, same standing wall as recent prompts) — flagged in LIVE_STATE for his confirmation. Queue is now empty.
- [CC | 2026-08-01 — Prompts 402/403/404 shipped, queue empty except HOLD] Overview You/Everyone toggle + role-aware attention (404, `84a91ce`), Overview clock now reads account timezone not device local (403, `0d542e7`), Leaderboard gets Production's date-nav stepper for browsing past periods (402, `0ed6702`). LIVE_STATE's "Next Up for CC" queue is empty except Prompt 393 (Zoom Live Room), still on HOLD pending Brayden's real Zoom URL. Full detail, verification notes, and resume prompts in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 407 shipped: profile avatars everywhere] `d46e661` + migration 096 (applied live, Brayden-confirmed). New shared Avatar component replaces 13 bespoke single-initial spots app-wide with real photo-or-two-initial-colored-circle rendering; Profile page gets click-to-upload. Build+lint clean; screenshot verification blocked by the same login wall as Prompts 401/403/404. Full detail + resume prompt in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 409 shipped: persistent avatar-upload camera badge] `0364e42`. Small follow-up to 407 — added a small always-visible camera-icon badge on the Profile avatar circle so it reads as clickable at rest, not just on hover. Found already partially written (uncommitted) on disk when picking up the queue; verified lint/build clean and shipped it. Full detail in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 408 shipped: Add-carrier form opens near the button] `8d84047`. Moved the Carrier Portals "Add carrier" form from below the full card grid to directly under the header row, so it's visible without scrolling. Pure JSX reorder, no logic change. Build+lint clean; screenshot verification blocked by the same login wall (Claude-in-Chrome also not connected this session). Full detail in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 406 shipped: Production defaults to Monthly] `e0f8aab`. Performance → Production's period toggle now opens on Monthly instead of All Time, matching Leaderboard. One-arg change (`usePeriodPicker(today, 'monthly')`) plus updated stale comments that documented the old default. Build+lint clean (one pre-existing, unrelated lint finding confirmed via git stash); screenshot verification blocked by the same login wall. Full detail in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 405 shipped: "Default Overview to" moved Settings→Profile] `54bca96`. Correction to Prompt 404 — the You/Everyone default-scope toggle only means anything once "I'm also actively writing business" is ON, so it moved off Settings → Regional (standalone) onto Profile, in the same section as the monthly-goal field, sharing that exact reveal gate. Build+lint clean (0 findings in both files); screenshot verification blocked by the same login wall. Queue now empty except Prompt 393 (HOLD). Full detail in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 410 shipped: Compensation Grid bigger text/rows] `815262e`. Increased table/header font size, row padding, and tier column width in `CompensationGrid.jsx` per Brayden's explicit tradeoff (readable text over fitting all 16 tiers without horizontal scroll). Skipped the softer secondary ask (own vertical-scroll box for the ~150 rows) — flagged as not fully committed and needs a live look to combine cleanly with the existing sticky-left columns. Build+lint clean; same login-wall screenshot block as recent prompts. Queue now just Prompt 393 (Daily.co Live Room, active). Full detail in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 393 shipped: Live Room built on Daily.co, blocked on Brayden's signup] `5344634`. Full Team → Meetings Live Room built with Daily's React SDK (custom join/mute/camera/tiles UI, real avatars when camera's off, admin "notify team" button) — everything CC can build is built and pushed. Genuinely stopped short of "done": CC cannot create accounts (hard policy), so this needs Brayden to sign up free at daily.co, create one room, and paste the URL into Settings → Integrations. No API key needed anywhere. Zero further code changes once that URL exists. Queue is now fully empty. Full detail in [[2026-Q3]].
- [CC | 2026-08-02 — Prompts 412+411 shipped: real Balance (projected commission) + tab reorder/rename] `a941f39` + fix `2a1facd`, migration 098 applied live. `policies.estimated_commission` now computed via trigger from `commission_schedule`'s tier-70 rate (exact carrier+product match), backfilled live — verified one real match ($739.20 on a Mutual of Omaha policy) and confirmed Aflac/Baltimore Life/Chubb correctly land on null → rendered as "Pending," never $0. New `Balance.jsx` replaces the ComingSoon placeholder; Commissions tabs reordered (Balance first/default) and renamed from "Balance & Reserve." Caught + fixed same session: the migration's function drop+recreate initially lost the avatar fields Prompt 407 had added to `team_performance_policies()` — corrected live before wrap-up. Build+lint clean; screenshot verification hit the same login wall (prod URL also 404'd this session) — build- and live-DB-verified only. Full detail in [[2026-Q3]].
- [CC | 2026-08-02 — Prompt 413 shipped: My Policies You/Everyone fixes + admin agent filter] `07cc83a`. Restyled My Policies' bespoke toggle to the standard `Segmented` component; wired it into the "I'm also actively writing business" gate Overview/Performance already respect (hides for admin when off, forces Everyone); added an admin-only "Filter by agent" option; renamed Profile's "Default Overview to" → "Default view" and wired that same setting into My Policies' and Performance's initial scope too (was Overview-only before). Build+lint clean (1 pre-existing unrelated finding confirmed via git stash). Screenshot verification blocked — same login wall, plus the Vercel prod URL itself 404'd in-browser for a second session running, flagged for Brayden to check directly. Build-verified only. Full detail in [[2026-Q3]].
- [CC | 2026-08-02 — Prompt 414 shipped: Performance header rows unified] `e9e2153`. Production's Daily/Monthly/All Time toggle moved from right-aligned to left (same slot as Leaderboard's own period toggle, same size); Leaderboard's caption + date nav merged from two loosely-spaced rows into one tight stack. Removed the now-unused `PeriodPicker` wrapper component. Build+lint clean. **Vercel prod URL 404'd a third session running** — flagged directly to Brayden, this needs a look outside CC's tooling. Confirmed the app itself boots clean via local dev server (rules out this change breaking anything); actual authenticated screenshot still blocked by the standing login-credential wall. Full detail in [[2026-Q3]].
- [CC | 2026-08-03 — Prompt 415 shipped: Training Center page shell] `64d4d7e`, migration 099 applied live. `/agent/training` replaced `TrainingPlaceholder` with real Script/Videos/AI Voice Roleplay tabs (Commissions.jsx's exact `Segmented` pattern). Script tab is real: contentEditable + `execCommand` rich-text editor (no editor lib in the repo), admin-write/everyone-read, autosaves to `app_settings.training_script` ~1.5s after typing stops with a `beforeunload` guard against unsaved-on-close. Videos/Roleplay reuse the shared `ComingSoon` component, zero new logic. Build+lint clean (0 findings in any touched file). Screenshot verification blocked by the same standing login wall (Claude-in-Chrome not connected) — build+lint+local-dev-boot-verified only. Full detail in [[2026-Q3]].
- [CC | 2026-08-04 — Prompt 416 shipped: Training Center Script tab locked] `3d0eafb`, migration 100 applied live (`app_settings.training_script_locked`, default true — existing saved script flipped to locked automatically, content untouched at 5,897 chars). Script tab now read-only for every role including admin when locked (no textbox, no Save button); admin-only "Unlock to edit" control reopens the existing editor; re-locking is an explicit "Save & lock" action rather than auto-relock-on-autosave (would've yanked the editor away mid-keystroke). Build+lint clean (0 findings in touched files, 55 pre-existing repo-wide issues unchanged). Screenshot verification blocked by the same standing login wall — build+lint+migration-verified-live+local-dev-boot-verified only. Queue now just Prompt 417 (Training Center Videos tab) plus Prompt 393 (Daily.co, HOLD). Full detail in [[2026-Q3]].
- [CC | 2026-08-04 — Prompt 417 shipped: Training Center Videos tab (YouTube embeds)] `ad89088`, migration 101 applied live. New `training_videos` table (same admin-write/everyone-read RLS shape as `carriers`); admin gets an "Add video" form (validates the pasted URL parses to a real video ID via new `lib/youtube.js`) plus per-card delete/reorder; everyone gets a thumbnail-grid that swaps in a real `youtube.com/embed/{id}` iframe inline on click — no upload/re-hosting. Build+lint clean (0 findings, 55 pre-existing repo-wide issues unchanged). **Full CRUD path verified live**, not just build-verified: inserted a real test video row via SQL, confirmed it round-tripped, then deleted it so the table starts empty for Brayden. Screenshot verification blocked by the same standing login wall — build+lint+migration-verified-live+local-dev-boot-verified only. Queue is now fully empty except Prompt 393 (Daily.co, HOLD). Full detail in [[2026-Q3]].
- [CC | 2026-08-04 — Prompt 418 shipped: Fulfillment Team routing (new role + handoff queue)] `e69bdd0`, migrations 102+103 applied live. New `fulfillment` profile role + additive `policies` columns (`fulfillment_assigned`/`fulfillment_stage`/`assigned_fulfillment_id`/`scheduled_call_at`); two new ADDITIVE RLS policies scoped to `fulfillment_assigned = true` (existing agent/downline/admin policies untouched); a `fulfillment_complete_notify()` trigger (same shape as migration 090's pattern) pings the original agent when Fulfillment marks a handoff complete. **Both `apply_migration` calls were blocked outright by the auto-mode classifier this session** (schema changes to prod) — resolved via AskUserQuestion, Brayden confirmed both explicitly, same precedent as Prompt 407. Submissions' New Submission form gets an off-by-default "Send to Fulfillment Team" toggle (self-write flow untouched when off) that swaps Policy #/Effective date/Underwriting decision out for a Callback-time picker. New `/fulfillment` route + `FulfillmentQueue.jsx` page (fulfillment role + admin only) with Claim/Mark Complete actions, wired into Sidebar/App.jsx/ProtectedRoute/roleLabels/Users.jsx role dropdowns/notification bell icon maps. Build+lint clean (0 findings in new/touched files; pre-existing hits confirmed unrelated via git stash). **Flagged rather than guessed:** the prompt's own "cancels the client's old policy" framing has no linking column between the new and old policy rows in the 5 scoped build items — Mark Complete only flips the new policy's status, so actually cancelling the replaced policy isn't wired into this flow. Didn't invent a fix for that gap; surfaced it in LIVE_STATE for Brayden. Screenshot verification blocked by the same standing login wall — build+lint+migration-verified-live+local-dev-boot-verified only. Queue is now empty. Full detail in LIVE_STATE's Prompt 418 CURRENT STATE entry.
- [CC | 2026-08-04 — Prompt 419 shipped: Fulfillment intake form + soft scheduling default + client-facing copy] `9fe86b9`, migration 104 applied live. **Directly answers the gap flagged at the end of 418** — new `policy_fulfillment_details` table (1:1 via `policy_id`) adds `current_carrier` (plain text, Brayden: "all you gotta do is call up the carrier and say I want to cancel this policy") plus everything else Fulfillment needs to write the replacement policy (legal name/DOB/address/DL/beneficiary/draft day/bank routing+account). RLS on this table is TIGHTER than 418's broad queue-listing policy — only submitting agent, the specific claimant, or admin can read it, so an unclaimed item's PII stays invisible to the rest of the fulfillment role. New Submission form gained an 18-field intake section (shown only when the 418 toggle is on) + a "Read to the client" script hint using "Underwriting Team" instead of "Fulfillment Team" (Brayden doesn't want agents saying the internal name) + a soft (non-blocking) scheduling nudge for callback times more than a day out — muted suggestion caption, inline confirm-or-dismiss banner at submit only. Fulfillment Queue page gained an expandable per-row detail panel (claimed-by-me-or-admin gated) with DL/routing/account masked to last-4 behind a reveal toggle. **Caught + fixed same session:** the far-out confirm button's first draft re-called `submit()`, which would've read a stale closure value of `confirmFarOut` (React state isn't synchronous) and shown the same confirm forever — split into `submit()` (gate) + `doSubmit()` (the confirm button calls this directly). Build+lint clean; all 3 new RLS policies confirmed live via `execute_sql`. Screenshot verification blocked by the same standing login wall — build+lint+migration-verified-live+local-dev-boot-verified only. Queue is now empty. Full detail in LIVE_STATE's Prompt 419 CURRENT STATE entry.
- [CC | 2026-08-04 — Prompt 420 shipped: fixed the REAL cause of "419's fields missing in production" — a stale PWA service worker, not a rendering bug] `425c1c8`. Read `Submissions.jsx`'s entire `sendToFulfillment` conditional chain — every gate (required-fields list, callback-time swap, the 18-field intake block) is correctly wired to the one boolean; nothing was wrong with 419's shipped code. Brayden's screenshot was actually running 418's bundle (callback time visible, policy#/underwriting hidden) while missing 419's fields — only explainable if his browser was executing older JS than what Vercel had live. **Root cause:** the app is an installed PWA (`vite-plugin-pwa`, `generateSW` mode, `registerType: 'autoUpdate'`) whose generated service worker binds every navigation to its own precached `index.html` — an already-registered SW keeps serving whatever it last precached, across hard refreshes included, until it independently notices a new deploy (browser SW-byte checks are lazy, up to 24h). `skipWaiting`/`clientsClaim` were already on, but nothing forced an active update check or a reload once a fresher SW took control — this bug was guaranteed to recur on every future deploy for any tab/PWA session left open across it, unrelated to what any given prompt builds. **Fix:** `main.jsx` now registers via `virtual:pwa-register`, polls `updateSW()` every 30 min, reloads once (guarded) on `controllerchange`; `vite.config.js` disables the old auto-injected registration to avoid double-registering. Build+lint clean (0 new findings, same 55 pre-existing). **Verified live in a real browser** (not just build-checked): `vite preview` on port 4173, opened in the Browser pane, confirmed the SW registers cleanly via the new path, zero console errors, no stuck waiting/installing state. Claude-in-Chrome still not connected this session (same gap as every prompt since 407) — could not get the specific authenticated 18-fields-visible screenshot the prompt demanded. **Brayden still needs to confirm**: fully close (not refresh) the Submissions tab/PWA session and reopen, since the tab already showing the bug is running the OLD stuck SW that hasn't picked up this fix's update-check logic yet. Full detail in LIVE_STATE's Prompt 420 CURRENT STATE entry.
- [CC | 2026-08-04 — session close, Prompt 420 confirmed clean] Both repos verified clean/pushed after the Prompt 420 fix (`ohvara-dashboard` @ `425c1c8`, `obsidian-mind` @ `455e7ed`) — nothing further to log this session. Waiting on Brayden to reopen his Submissions tab fresh and confirm the 18 intake fields now render before this prompt can close.
- [CC | 2026-08-05 — Prompt 420 confirmed fixed by Brayden; Prompt 421 shipped: Fulfillment callback booking is now a date+slot picker] `6d2e7b2`. New session opened with Prompt 420 already resolved (Brayden's own screenshot confirmed all 18 intake fields render correctly post-fix). Picked up the next queued item: swapped the Fulfillment intake's raw `datetime-local` "Callback time" field for the same pick-a-date-then-pick-a-fixed-hourly-slot UI the existing Cancellation Calendar tab already uses. Extracted `SLOTS`/`slotToISO` out of `CancellationCalendar()` into new shared `src/lib/scheduling.js` so both booking flows share one implementation instead of drifting copies, per the prompt's explicit instruction. `form.scheduled_call_at` stays the single value of record (still an ISO string) — new `callbackDate`/`callbackSlot` picker state just recomputes it via `slotToISO()`, so `validate()`/`submit()`/`doSubmit()` and the existing far-out confirm-banner gate needed zero changes. Reused Cancellation Calendar's exact `GapNote` copy about no real capacity checking, per the prompt's instruction not to build that for one flow and not the other. Build+lint clean (0 new findings, same 55 pre-existing repo-wide — confirmed the one stray lint warning in this run is pre-existing in unrelated `NotificationToast.jsx`). Local dev server boots with zero console errors. **Could not get the live screenshot the prompt explicitly demanded** (login-gated form; Claude-in-Chrome checked twice this session, not connected either time) — ships build+lint+local-dev-boot-verified only, flagged to Brayden directly in LIVE_STATE for a confirming screenshot, same closing pattern as 420. Full detail in LIVE_STATE's Prompt 421 CURRENT STATE entry.
- [CC | 2026-08-05 — Prompt 422 shipped: avatar crop/zoom on upload + Remove photo] `c1788d8`. Closed the two gaps left in the avatar system since 407/409: photos uploaded as-is (off-center/stretched), and there was no way to clear one once set. Added `react-easy-crop` wired into a new circular crop/zoom modal; on confirm, a new `lib/imageCrop.js` renders the cropped rect into a fixed 512×512 canvas and uploads that Blob through the existing upload path unchanged. **Caught a real build failure, not just a warning:** the new library pushed the main bundle just over vite-plugin-pwa's hard 2 MiB precache ceiling, so `npm run build` failed outright. Fixed it properly with `React.lazy()`/`Suspense` code-splitting (the crop modal + library now load in their own ~27kB chunk only when opened) instead of just raising the size limit, since the library is only ever needed in this one rarely-triggered modal. New `useRemoveAvatar` hook lists the user's actual storage folder (not a fixed-extension guess) and deletes every file found before nulling `avatar_url` — existing delete RLS from migration 096 already covers it, no new policy needed. Build+lint clean (0 new findings, same 55 pre-existing); local dev server boots clean. **Could not get the 3 screenshots the prompt asked for** — login-gated page, Claude-in-Chrome still not connected (checked again). Ships build+lint+local-dev-boot-verified only, flagged to Brayden in LIVE_STATE, same closing pattern as 420/421. Full detail in LIVE_STATE's Prompt 422 CURRENT STATE entry.
- [CC | 2026-08-05 — Prompt 423 shipped: New Submission rewritten, no more Fulfillment toggle] `7d5c953`, migration 105 applied live (Brayden's explicit go-ahead, same AskUserQuestion precedent as 407/418). Brayden reversed his earlier "keep the toggle" call after seeing it live — removed the `Switch` entirely along with the three self-write-only fields it used to gate (Policy #, Effective date, Approved/In Underwriting); the form is now unconditionally the Fulfillment intake, `validate()`/`submit()`/`doSubmit()` no longer branch on anything. Fixed the duplicate-name bug: the separate "Full legal name" intake field is gone, `policy_fulfillment_details.full_legal_name` now derives from Client First/Last Name on submit. Callback time moved from mid-form to the very end, right before Log Submission. Beneficiaries went from flat `beneficiary_name`/`beneficiary_relationship` columns to a `beneficiaries jsonb` array (table had 0 rows live, checked via `execute_sql` before writing the migration — clean swap) — New Submission now supports multiple beneficiary rows with a Spouse/Child/Parent/Sibling/Other relationship dropdown, "+ Add another beneficiary," per-row Remove once there's more than one; Fulfillment Queue's detail panel updated to render the array. Build+lint clean (0 new findings, same 55 pre-existing); local dev server boots clean; migration verified live via `execute_sql`. **Could not get the 2 screenshots the prompt demanded** (full form top-to-bottom, two beneficiary rows) — same standing Claude-in-Chrome gap, checked again this session. Ships build+lint+local-dev-boot-verified+migration-verified-live only — **flagged for a closer-than-usual look**, this is the biggest structural rewrite this form's had (toggle removed, fields reordered, new multi-row UI). Queue is now empty. Full detail in LIVE_STATE's Prompt 423 CURRENT STATE entry.

