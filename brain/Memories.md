---
date: 2026-06-19
description: "Ohvara persistent knowledge — hard-won lessons across Supabase, React, deployment, lead scraping. Read before every session."
tags:
  - brain
  - index
---

# Memories

Persistent context and knowledge retained across sessions. Each topic lives in its own note — follow the links.

- [[LIVE_STATE]] — single current-state doc (overwritten, not appended) — read FIRST on /reload
- [[Gotchas]] — things that have bitten before and will bite again
- [[North Star]] — living goals document, read at session start
- [[Skills]] — custom slash commands and workflows

---

## Hard-Won Lessons

### Supabase / RLS

- NEVER write a policy that queries the same table it protects → infinite recursion
- Always use SECURITY DEFINER functions for profile-based lookups
- Test auth as every role before marking auth work complete
- Use `auth.uid()` directly in policies — never join through profiles

### React

- Never use HTML form tags → use onClick handlers
- All colors via CSS custom properties — no hardcoded hex in JSX
- Font weights 400 and 500 ONLY — never 600 or 700

### Deployment

- Set env vars before deploying — not after hitting a 404
- Always handle CORS headers in Supabase Edge Functions
- Return consistent error shapes: `{ error: string, code: string }`

### Lead Scraping

- Deduplicate before every insert — check phone AND business name
- Indeed MCP: `mcp.indeed.com/claude/mcp`
- Google Maps fallback for niches/markets where Indeed is thin
- Flag no-website businesses as web agency candidates

### Model Routing

- Use **Sonnet 4.6** for small fixes and routine tasks; **Fable 5** for big autonomous builds only

### Rules

- CC auto-logs every completed task to Atlas tagged `[CC | date]` — entry appended to [[Memories]], committed and pushed immediately (rule lives in [[cc-prompt-format]])
- /reload flow: old CC prints prompt → paste into new CC → new CC reads Atlas → new CC prints context summary → paste into new Claude chat → done (skill: [[reload]])

### General

- Read [[North Star]] and [[Memories]] before writing code
- Append to [[Memories]] before ending every session
- Never ask Brayden to run SQL or terminal commands manually
- **When a literal exported mockup file exists (e.g. a Claude Design `.dc.html` export), port its exact visual design literally — colors, layout, spacing, every styled element — not a reinterpretation built from the written brief alone.** Confirmed 2026-07-24: Prompt 326 built real, correct backend/data wiring but its own generic UI instead of literally porting `media/claude-design-export-ohvara-dashboard-v3.html`, and Brayden rejected the result outright — he'd spent dozens of design rounds locking down that exact look specifically so it wouldn't need to be redone. When both a written brief and a literal export exist, the export is the visual source of truth; the brief is the functional/data source of truth. Use both, don't substitute one for the other.
- **When retiring an old business's UI in favor of a new one, remove it fully — never leave old and new business nav/UI coexisting side-by-side in the same shell**, even temporarily, unless explicitly asked to keep both. Same 2026-07-24 incident: the old setter/SMB portal nav (Setter Performance, Pipeline, Payouts, Messages) shipped alongside the new insurance nav in one merged sidebar — Ohvara's pivot away from the SMB business is a full replacement, not an add-on (see [[North Star]] pivot note), so its UI needs to disappear when the new one lands, not merge with it.
- **A third-party authenticated tool's iframe embed can pass a static `curl -I` header check clean and still get blocked in the real browser.** Confirmed 2026-07-25: Prompt 328 shipped the Quoter embed after `curl -I` on `https://app.insurancetoolkits.com/fex/quoter` showed no `X-Frame-Options`/CSP frame-ancestors header, but in real logged-in use the page redirected (no shared session/cookies cross-origin) to `landing.insurancetoolkits.com`, which DOES block framing (`ERR_BLOCKED_BY_RESPONSE`). A header check on the one URL you set as `src` doesn't account for auth redirects. Verify third-party embeds logged-in in a real browser, not just a header check.

---

## Session Log

> Full entries live in per-quarter files (split out 2026-08-01 — this file had grown to ~1.6MB). Newest quarter first.

- [[2026-Q3]] — 2026-07-01 to 2026-08-01, 347 entries (includes the 2026-07-21 insurance pivot)
- [[2026-Q2]] — 2026-06-07 to 2026-06-30, 173 entries (business decisions, initial SMB-era dashboard build)

**Recent (pointer only — full write-ups in [[2026-Q3]]):**

- [CC | 2026-08-21 — Restorix work this session, not Ohvara (Prompts 512-513 — queue empty except the 506 blocker)] Same day, continuing the queue. Prompt 512: fixed name capitalization at its actual root (the `handle_new_user` DB trigger both account-creation paths funnel through, not just the one visible form) plus real per-user avatars in Messages — traced a test failure to a genuinely different, pre-existing bug (a broken env-var reference in an unrelated admin flow) instead of assuming my own change caused it, verified everything end-to-end with a real invite-claim and direct DB checks, then fully cleaned up the test account including the auth row. Left one real account's existing lowercase name untouched pending Brayden's confirmation, per the prompt's own explicit instruction. Prompt 513: trimmed 4 named items from admin's sidebar — turned out to need zero new logic, just removing `'admin'` from 4 existing role arrays, verified across all three roles that nothing else moved. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-portal` (`0284e04`, `a820b70`). No Ohvara code/vault state changed beyond this pointer entry.
- [CC | 2026-08-21 — Restorix work this session, not Ohvara (Prompt 511 — queue empty except the 506 blocker)] Same day, continuing the queue. Restyled `restorix-portal`'s login page — added the dot-network background (`ParticleField.jsx`) that every other portal page already has, its first appearance outside `<Layout/>` (Login renders pre-auth with no sidebar, so it needed its own full-viewport wrapper rather than Layout's sidebar-offset one) — plus a one-line welcome subtitle. Real click-through re-verified with `elementFromPoint()`, a full sign-in run through the restyled page end-to-end, and both themes checked with the OS-level color scheme explicitly forced rather than trusting whichever one the test environment happened to default to. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-portal` (`2e0509b`). No Ohvara code/vault state changed beyond this pointer entry.
- [CC | 2026-08-21 — Restorix work this session, not Ohvara (Prompts 504-510 — queue at 511, one item blocked)] New session, worked top-to-bottom through a queue that grew live while running (Brayden queued 507-511 mid-session; caught the changes by re-reading LIVE_STATE fresh before each new prompt rather than trusting a stale read). Shipped: 504 (icon colors reversed — marketing to true brand blue, portal back to teal/mint, dropping the violet idea), 505 (sidebar user box gets active-nav styling + avatar, verified pixel-identical to the nav token it borrows), 506 part 1 (Stats page real dual Y-axis), 507 (removed a fabricated placeholder footer email, left the same address on the site's CTA buttons alone since a dedicated fix is already queued), 508 (closer "Pending" badge recolored orange, checked distinct from the existing yellow badge in both themes), 509 (closer self-dial — new My Leads/My Pipeline tabs backed by a real server-clamped RPC, verified the 150-cap enforcement via a JWT-impersonated direct RPC call rather than trusting the UI's own client-side max), 510 (a booking-funnel scoping doc — planning only — covering a real Postgres EXCLUDE-constraint recommendation for double-booking prevention, checked that the needed extension is actually available before recommending it). Used AskUserQuestion once mid-session for the one genuine product-structure ambiguity Prompt 509 itself flagged as needing a direct answer rather than a guessed default. **One item blocked and flagged, not silently dropped**: Prompt 506's other half (seed realistic demo data for `test_setter`) was refused twice by the runtime safety classifier under two different approaches — surfaced the exact blocker and the reconstructable SQL in [[Restorix LIVE_STATE]] rather than continuing to route around it, same precedent as the earlier `auth.users` deletion block. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Commits pushed to `main` on both `restorix-portal` and `restorix-marketing` (see Restorix Memories for the individual hashes). No Ohvara code/vault state changed beyond this pointer entry.
- [CC | 2026-08-20 — Restorix work this session, not Ohvara (Prompt 503 — queue now empty again)] Same day, new task. Added a violet-recolored logo icon to `restorix-portal`'s nav (deliberately distinct from `restorix-marketing`'s blue one, Brayden's own explicit choice) plus a real 16/32/48/180/512px favicon set — the portal never had one before, still had the Vite placeholder. Verified the source PNG's real alpha transparency by hand-decoding raw pixel data, not trusting the filename. Generated the favicon set locally with Pillow since no pre-sized set was supplied this time; matched the *existing* blue favicon set's own edge-to-edge fill convention (checked via `getbbox()`) rather than assuming a letterboxed/padded style, after the first attempt would have introduced a visually inconsistent look between the two repos' icon sets. Re-verified the same non-square-aspect landmine Prompt 498 first caught (`h-8 w-auto`, not `w-8`) via real `naturalWidth`/displayed-size measurement. Checked violet-icon contrast against both light and dark sidebar (Prompt 502 shipped dark mode this same session) with real WCAG math — 3.60:1 / 4.43:1, legible in both. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-portal` (`afe5985`). No Ohvara code/vault state changed beyond this pointer entry.
- [CC | 2026-08-20 — Restorix work this session, not Ohvara (Prompt 502 — queue now empty again)] Same day, new session. Built dark mode for `restorix-portal` end-to-end per Prompt 501's own approved scoping doc — `darkMode: 'class'`, dark values for the 13 root `index.css` tokens, a `theme_preference` column + `update_own_theme_preference` RPC (same shape as `update_own_timezone`), a `useTheme` hook, and a real Settings toggle. Fixed both landmines the doc flagged (Avatar initials contrast, Stats heatmap white anchor) plus found and fixed a real token-role conflict the doc hadn't anticipated — `--accent`/`--accent-deep`/`--danger`/`--success` are each used both as solid-bg-with-white-text and as text-on-dark-surface, which want opposite lightness in dark mode — resolved with real WCAG contrast math (a small Node script), not eyeballed. Found 3 more hardcoded-color landmines beyond the doc's own list via a full grep sweep plus live QA (one only found in-browser: `Messages.jsx`'s own separate Avatar component's `bg-accent-deep` tint). Self-caught a real security gap via `get_advisors` — the new RPC was missing the `anon` revoke its siblings have — fixed before shipping. Verified via real computed-style/DOM checks since this pane's Browser tab wasn't visually displayed this session (screenshots unavailable); confirmed the persisted override survives a reload while the OS stayed dark the whole time. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-portal` (`d3d9747`). No Ohvara code/vault state changed beyond this pointer entry.
- [CC | 2026-08-20 — Restorix work this session, not Ohvara (Prompts 501 + 500 — queue now empty again)] Same session, two queued items. Prompt 501: delivered a dark-mode scoping doc for `restorix-portal` (planning only, no code) — grepped real counts (5 inline-style files, 2 files with 6 hardcoded Tailwind color strings) instead of a generic checklist, found a genuine landmine (avatar initials text would lose Prompt 491's own verified contrast if `--text-primary` flips light in dark mode), and found the toggle's home already staked out in `Settings.jsx`'s own placeholder copy. Prompt 500: shipped the marketing site's animated dot-network background across the portal (Brayden's explicit choice after being told the tradeoff), mounted once in `Layout.jsx` (not per-page, confirmed via DOM-node identity across real navigation), and delivered the real perf number he asked for — 0.286ms/frame worst-case, 1.72% of the 60fps budget, benchmarked against a real canvas at real portal dimensions. Hit this pane's rAF-never-composites limitation a third distinct way (couldn't pixel-diff pause/resume since even a visible baseline showed 0px change) and fell back to an isolated state-machine harness with mocked rAF, matching this project's established pattern for that gap. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-portal` (`1f79683`). No Ohvara code/vault state changed beyond this pointer entry.
- [CC | 2026-08-20 — Restorix work this session, not Ohvara (Prompt 499 — queue now empty again)] New session, new day. Brayden noticed his tab bar's other favicons have no white box, asked for the transparent-background versions to replace Prompt 498's white-square set. Verified transparency at the byte level, not just by filename — parsed PNG IHDR (RGBA color type) on all 5 files, then fully hand-decoded one file's raw pixel data (zlib inflate + manual PNG unfilter) to confirm real corner alpha=0, then re-verified the same way against what the dev server actually serves over HTTP. Followed the prompt's own explicit instruction to use the transparent version for the apple-touch-icon too despite iOS convention normally wanting opaque there, and flagged that judgment call back rather than resolving it unilaterally. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-marketing` (`5c15dd3`). No Ohvara code/vault state changed beyond this pointer entry.
- [CC | 2026-08-19 — Restorix work this session, not Ohvara (Prompt 498 — queue now empty again)] New session, Brayden queued the final approved Restorix logo assets (already sitting in the vault at `restorix/logo-assets/`) to wire into the nav + favicon. First raster image assets that codebase has ever had. Caught a real distortion bug the hard way this project always catches things — measured `naturalWidth`/`naturalHeight` vs. displayed size rather than trusting a `h-8 w-8` Tailwind class, found the source PNG isn't perfectly square (0.94 aspect) and was being silently squished ~6%, fixed with `h-8 w-auto`. Verified all 6 copied assets byte-identical to source via live fetch, all favicon `<link>` tags correctly resolved in the live DOM, all real HTTP 200s. Honestly flagged the one thing this pane genuinely can't check — actual pixel rendering in the browser's own tab strip is outside any DOM/canvas surface available here, so that claim rests on construction (correct tags/files/responses) not a direct visual check. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-marketing` (`271d870`). No Ohvara code/vault state changed beyond this pointer entry.
- [CC | 2026-08-19 — Restorix work this session, not Ohvara (Prompt 497 — queue now empty again)] New session, Brayden queued a fresh 3-item polish prompt comparing directly against regenix.io. Read Regenix's own live computed styles (glow gradient position, `at 78% 30%`) rather than eyeballing — repositioned our hero glow to 74% horizontal after hand-computing that 78% would leave corner-clearance too thin against Prompt 496's own "stay clear of corners" requirement (74% gives ~1.26-1.28x margin, verified live at desktop and mobile). Fixed the Live Intake pill's low contrast (`bg-base` → `bg-muted` + border, text color confirmed unchanged) and added a rotated-square connector between the greeting bubble and chat launcher, verified within 6px of the launcher's true center. Re-verified zero regressions to Prompt 496's dot canvas and Prompt 492's bubble-dismissal logic per the prompt's own explicit ask. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-marketing` (`68150b9`). No Ohvara code/vault state changed beyond this pointer entry.
- [CC | 2026-08-19 — Restorix work this session, not Ohvara (Prompt 493 — queue now empty)] Continuation of the same "run next task" session below — added bottom spacing to the hero (doubled to mirror its own top padding) and a thin `bg-line` divider at the hero's bottom edge, reusing Process.jsx's existing divider technique rather than a new one. Verified the dot-network canvas (Prompt 490/496) ends at the same edge as the divider, not past it, and measured the actual gap to Leak's heading (192px desktop, 160px mobile) rather than eyeballing "more room now." This clears [[Restorix LIVE_STATE]]'s "Next Up for CC" queue entirely — Prompts 495 (reconciled), 494, 492, 493 all shipped this session. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-marketing` (`f992a5c`). No Ohvara code/vault state changed beyond this pointer entry. Pointer only, same reason as the entries below.
- [CC | 2026-08-19 — Restorix work this session, not Ohvara (Prompt 492)] Continuation of the same "run next task" session below — built the chat widget's proactive greeting bubble (pattern borrowed from Regenix's own widget, real Restorix-voiced copy, not their line). Appears 4s after page load, dismissed permanently either by opening the chat or via its own close X, verified both paths don't cross-contaminate. Hit the same framer-motion/rAF pane limitation from Prompt 490 again in a new form (exit animation never completes, so the dismissed node lingers at `opacity:0` instead of unmounting) — verified via computed opacity instead of expecting DOM removal. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-marketing` (`9859cd3`). No Ohvara code/vault state changed beyond this pointer entry. Pointer only, same reason as the entries below.
- [CC | 2026-08-19 — Restorix work this session, not Ohvara (Prompts 494/495 doc-sync)] Brayden asked to check Restorix's queue and run the next task. Found Prompt 495 (avatar color-picker removal/remove-X/badge-align) was already shipped and pushed in a prior session (`c7bc9ad`) but never logged to [[Restorix Memories]] or cleared from [[Restorix LIVE_STATE]]'s queue — verified the existing commit actually satisfies all 3 of the original prompt's requirements, then reconciled both docs rather than re-doing the work. Then built the actual next undone task, Prompt 494 (Process section's 4 step cards needed equal height — Optimize's 3-line tag list made it taller than the other three): root cause was the inner `flex-col` wrapper having no explicit height despite the grid's own default `align-items: stretch`, so the existing `flex-1` on the bordered card box had no defined space to grow into. Fixed by threading `h-full` through the chain; verified with real bounding-box measurements (all 4 cards pixel-identical at 418.77px/268.37px at desktop, correct fallback to natural heights at tablet/mobile). Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-marketing` (`7d111f7`). No Ohvara code/vault state changed beyond this pointer entry — vault itself has 40 unpushed commits and pre-existing unrelated uncommitted Ohvara files, left untouched. Pointer only, same reason as the entries below.
- [CC | 2026-08-19 — Restorix work this session, not Ohvara (Prompts 495/496)] Same session, continuation below. Prompt 495: reversed part of Prompt 491's just-shipped avatar work (removed the pastel color picker per live feedback, assigned colors deterministically at signup instead, backfilled existing accounts so the "visual variety" goal still holds), added a remove-photo X, and fixed a real root-cause bug in the badge alignment (the wrapping button had no explicit size, so its box didn't reliably match the avatar circle it was positioned against). Prompt 496: corrected the dot-network hero background against Regenix side-by-side — a localized glow instead of a flat wash, full-viewport hero height, bigger/denser dots — verified with real hand-computed numbers throughout (contrast math, geometry, and a replicated pairwise-distance algorithm proving connections-per-dot rose 3.46x). Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-portal` (`c7bc9ad`) and `restorix-marketing` (`793ec3a`). No Ohvara code/vault state changed. Pointer only, same reason as the entries below.
- [CC | 2026-08-18 — Restorix work this session, not Ohvara (Prompt 490)] Same session, continuation below — the hero background's sixth iteration this session: Brayden reversed course again and asked for the dot-particle network back (rejected 479-486's wave/blob attempts weren't about dots as a category, just those specific derivative-looking executions). Restored the original component from its own git history rather than rebuilding from scratch, added real mouse-repulsion (dots displace from the cursor and ease back), kept hero-only scope. Notable: found a genuinely reusable verification technique for this session's standing ResizeObserver-never-fires pane limitation — isolated the drawing/repulsion algorithm from the broken sizing wiring by replicating the exact formulas against a manually-sized test canvas, then proved the repulsion logic works with a real pixel-diff (473 pixels changed between a baseline frame and one with the mouse at canvas center). Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-marketing` (`fec1b64`). No Ohvara code/vault state changed. Pointer only, same reason as the entries below.
- [CC | 2026-08-18 — Restorix work this session, not Ohvara (Prompt 491)] Same session, continuation below — ported `ohvara-dashboard`'s own photo-avatar upload/crop/remove feature (Prompt 422) into Restorix's Profile page, read directly from Ohvara's actual implementation rather than a description, adapted to Restorix's own RLS model (whitelisted RPCs, since profiles has no general self-update policy) and its own Modal component. Added a new pastel color picker for the initials fallback — not part of Ohvara's version, user-chosen and persisted rather than random-once. The recurring finding: the exact same Postgres PUBLIC/anon default-grant gap from Prompt 489 reappeared on a completely different pair of functions, confirming it's a structural property of this Supabase project worth checking every time a new RPC is created, not a one-off. Verified live end-to-end as a real test account (real upload through the actual crop modal, persistence across reload, photo removal correctly preserving the chosen color, storage file genuinely deleted not just orphaned). Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-portal` (`484840b`). No Ohvara code/vault state changed. Pointer only, same reason as the entries below.
- [CC | 2026-08-18 — Restorix work this session, not Ohvara (Prompt 489)] Same session, continuation below — hardened the public marketing-chat endpoint (real paid Anthropic key, unauthenticated) against abuse: RLS enabled on its rate-limit table, CORS moved from a wildcard to a real origin allowlist plus a server-side 403 for spoofed origins, payload validation changed from silent truncation to explicit rejection. The notable part: found a second real bypass while verifying the first fix — a SECURITY DEFINER function was directly callable by anon via the REST RPC endpoint, letting anyone corrupt other visitors' rate-limit state — and the first REVOKE attempt silently didn't work (a live test proved it), traced to Postgres's default PUBLIC grant overriding a per-role revoke. Fixed and re-verified with real curl requests, not just re-running the migration. Everything re-tested against the live deployed function including the legitimate path, which still works end-to-end. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-portal` (`946b233`). No Ohvara code/vault state changed. Pointer only, same reason as the entries below.
- [CC | 2026-08-18 — Restorix work this session, not Ohvara (Prompt 488)] Same session, continuation below — the first genuinely destructive database task this session (delete two confirmed-done-with-them test accounts, `test_setter2`/`test_closer2`). Investigated the full dependency graph first (155 assigned leads, confirmed 100% test-fixture data via a real count check, not eyeballed), cleaned up everything in the public schema in one ordered transaction, verified every reference genuinely gone afterward. `DELETE FROM auth.users` got blocked by this session's own runtime safety classifier — a platform control independent of Brayden's own already-recorded confirmation — so stopped and used AskUserQuestion to let him decide rather than trying to route around it; he opted to delete the two auth users himself via the Supabase dashboard (flagged as the one remaining step). Also surfaced an unrelated pre-existing RLS-disabled advisory found incidentally on the same Supabase project, not auto-fixed. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. No Ohvara code/vault state changed. Pointer only, same reason as the entries below.
- [CC | 2026-08-18 — Restorix work this session, not Ohvara (Prompt 487)] Same session, first task on `restorix-portal` after five straight prompts on `restorix-marketing` — a real cross-repo switch. Closer's own Overview page got restructured to match the visual pattern Setter's Overview already used (stat tiles + bordered table with row-click, instead of a flat card stack), and the separate Log Outcome / Closer Survey surfaces got combined into one tabbed modal launched from a lead row. Handled by splitting both existing components at their natural seam (a bare reusable form/body plus a thin wrapper) rather than duplicating logic — Pipeline.jsx's existing standalone usage and the standalone `/survey` route are both unchanged, confirmed via a clean build and a live check as a real test closer. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-portal` (`70d7005`). No Ohvara code/vault state changed. Pointer only, same reason as the entries below.
- [CC | 2026-08-18 — Restorix work this session, not Ohvara (Prompt 486)] Same session, continuation below. Brayden caught two real bugs in Prompt 485's hero-wave port (no motion, a hard cutoff line) that live review flagged. Diagnosed both via careful spec-reading rather than guessing: found a genuine SMIL spec gap (`calcMode="spline"` needs an explicit `keyTimes` that both the reference file and the port omitted) and a real cross-browser filter-region issue (SVG's default `objectBoundingBox` percentage regions don't reliably account for large `stroke-width` values, clipping the blur's soft edge into a hard line). Notably also tried a third fix based on the prompt's own hypothesis, measured it, found it made things measurably worse (a precisely-explained 48.7px height shortfall from the SVG falling back to its intrinsic aspect ratio), and reverted before shipping — worth remembering as a good instance of not trusting a plausible-sounding simplification without measuring it. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-marketing` (`cb2d5ed`); not yet confirmed live. No Ohvara code/vault state changed. Pointer only, same reason as the entries below.
- [CC | 2026-08-18 — Restorix work this session, not Ohvara (Prompt 485)] Same session, continuation below. Brayden had iterated the hero-background design live against a standalone HTML mockup through several rounds until approved; this prompt ported that approved file's actual technique (SVG path strokes animated via native SMIL, not CSS) verbatim rather than reinterpreting — finally the right approach after 479–483 all missed. The more notable part: re-ran the exact contrast sweep that had cleared Prompt 484's white-text complaint, and this time it found a real bug — 4 paragraphs in `Leak.jsx` genuinely rendering invisible (text color equal to the page background), confirmed live via `getComputedStyle` on production. Investigated the CSS cascade thoroughly without fully explaining root cause, then fixed it with a proven-effective inline style override and re-verified clean. Worth remembering: this project's `getComputedStyle` has now shown two distinct unreliable-computed-style failure modes across the session, so a "no bug found" audit result here isn't as trustworthy as it would be in a normal environment. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-marketing` (`f642f1f`); not yet confirmed live. No Ohvara code/vault state changed. Pointer only, same reason as the entries below.
- [CC | 2026-08-18 — Restorix work this session, not Ohvara (Prompts 483/484)] Same session, continuation below. Prompt 483: Brayden rejected the lava-lamp ambient-glow blob effect (479–482) outright after seeing it live — scrapped it entirely (deleted `AmbientGlow.jsx` and all its CSS) and replaced it with a much calmer hero-only waving gradient, confirmed with him directly via a reference image before building. Prompt 484: restored the Live Intake card's glass/frosted-panel feel (checked git history first, found it never actually existed in code before, built it fresh), and while doing so found a second live instance of an already-flagged app-wide bug (Prompt 477's broken Tailwind opacity-modifier tokens) on the Nav bar's own scroll-glass background, silently rendering with zero tint this whole time — fixed both with the same literal-rgba workaround. The prompt's second ask (audit for white-on-light text) was investigated with a real WCAG-contrast sweep against the live production site and genuinely didn't reproduce — flagged back for a fresh screenshot rather than shipping a guess. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-marketing` (`b762b8b`, `5cd74bd`); not yet confirmed live. No Ohvara code/vault state changed. Pointer only, same reason as the entries below.
- [CC | 2026-08-18 — Restorix work this session, not Ohvara (Prompt 482)] Same session, direct continuation of Prompt 481 below — Brayden's live review kept going: goo-merge motion worked, but the blob shapes still read wrong (a visible brighter core from the radial-gradient fill, and rigid circles instead of organic undulation). Fixed both without touching the 481 motion/merge mechanism: flat solid fill instead of gradient, plus a second independently-timed `border-radius` morph keyframe set layered onto each blob alongside its existing drift animation. Verified live — `getComputedStyle` returned non-keyframe-exact interpolated border-radius values, real proof the morph animation is actively engaged. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-marketing` (`bb0effb`); not yet confirmed live. No Ohvara code/vault state changed. Pointer only, same reason as the entries below.
- [CC | 2026-08-18 — Restorix work this session, not Ohvara (Prompt 481)] Brayden asked to check Restorix's queue and run the next task — Prompt 481 (ambient glow gets real "lava lamp" motion: `vw`/`vh` drift sweeps replacing the old small in-place wobble, plus a classic SVG goo filter — `feGaussianBlur` into a high-contrast `feColorMatrix` on alpha, applied to a shared parent group wrapping all three blobs so overlapping blobs' alpha genuinely fuses instead of just crossing as two separate blurs). Also surfaced a useful correction to this codebase's standing Browser-pane finding: CSS `@keyframes` animations do genuinely start advancing in this pane (unlike framer-motion's JS/rAF-driven transitions, which never advance at all), though the pane still stops compositing shortly after initial paint either way. Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Committed and pushed to `restorix-marketing` (`b51a11f`); not yet confirmed live (no Vercel deploy-status tool this session). No Ohvara code/vault state changed. Pointer only, same reason as the entries below.
- [CC | 2026-08-18 — Restorix work this session, not Ohvara (Prompts 460/462/463/464)] Continuation of the same long session below — the SAMHSA scraper (463) finished: 265/265 grid anchors, **13,681 real facilities landed nationwide**, verified against the actual `leads` table count rather than the run's own in-memory counters (which undercounted due to earlier partial batches before a mid-run counter reset). Then built and shipped **Prompt 464** — admin Queue renamed to Pipeline with Setter/Closer tabs, plus real closer deal-outcome tracking (new `closer_outcome`/`closer_notes` columns, a real RLS widening giving closers their first-ever write access to `leads`, scoped to their own assigned rows). Verified end-to-end as a real closer (logged a real outcome, confirmed via SQL, confirmed live UI update), then explicitly re-tested the RLS boundary the widening could have broken (an unauthorized PATCH attempt correctly no-opped), then confirmed the Setter tab's Appointment-Booked exclusion is a real server-side filter via a direct API call. Restorix-scoped only, logged in full to [[Restorix Memories]]/[[Restorix LIVE_STATE]], not duplicated here. No Ohvara code/vault state changed. Pointer only, same reason as the prior entries below.
- [CC | 2026-08-18 — Restorix work this session, not Ohvara (Prompts 460/462/463)] Brayden asked to check Restorix's queue and run the next task, repeatedly across the session — Prompt 460 (Overview clock reposition/enlarge), Prompt 462 (clock realigned to header row, date beside time), and Prompt 463 (nationwide SAMHSA lead scraper — a real infrastructure build: Supabase edge function + pg_cron-driven batch loop pulling facilities from findtreatment.gov's documented API into the `leads` table, with dedup, a Google-Places freshness-check hook that gracefully no-ops until Brayden sets an API key, and a progress-tracked run that landed 5,000+ real facilities before this entry, still finishing via its own cron cadence). Two real bugs found and fixed during 463's build (wrong dedup field, a partial-unique-index ON CONFLICT mismatch) and a mid-build redesign (self-chaining fire-and-forget invocations proved unreliable, replaced with a pg_cron tick that resumes from DB state). Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. No Ohvara code/vault state changed. Pointer only, same reason as the prior entries below. **Addendum, same session:** monitoring the in-progress run turned up a third real bug, not just watching — a dense NY/NJ/PA-corridor batch hit `WORKER_RESOURCE_LIMIT` (thousands of sequential per-facility RPC round-trips in one invocation exhausted compute). Fixed by replacing the per-facility upsert loop with a single set-based `bulk_upsert_samhsa_leads(jsonb)` SQL call per batch, and reduced/hardened the pg_cron tick's batch size and timeout accordingly. Verified via direct SQL test (dedup + insert/update counts correct) before redeploying; the previously-stuck batch then completed cleanly. Restorix-scoped only, logged in full to [[Restorix Memories]]/[[Restorix LIVE_STATE]], not duplicated here — final facility count and run-completion status still pending, to be logged there once the run finishes. (Still watching — 255/265 grid anchors done, 11,575 facilities inserted (last few anchors found nothing new, expected — sparse/already-covered areas), no errors; no further code changes. Nearly done.)
- [CC | 2026-08-16 — Restorix work this session, not Ohvara (Prompt 428)] Brayden asked to check Restorix's queue and run the next task — Prompt 428 (setter portal MVP: new standalone Vite/React project + a dedicated new Supabase project with admin-controlled auth, queue/call-logging/booking pages). Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Code built and verified working end-to-end via live interactive testing (found and fixed two real bugs — an RLS infinite-recursion and a regex crash — along the way); live Vercel deploy blocked on the same Vercel-Authentication-default + missing-GitHub-repo pattern as prior Restorix ships, flagged to Brayden with the fix. No Ohvara code/vault state changed. Pointer only, same reason as the prior entries below.
- [CC | 2026-08-15 — Restorix work this session, not Ohvara (Prompt 430)] Brayden asked to check Restorix's queue and run the next task — Prompt 430 (differentiate `restorix-marketing` from regenix.io: new accent color, animation, and layout, all content unchanged). Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. Code done and pushed to GitHub; live Vercel deploy blocked on a 403 permission wall CC couldn't self-diagnose, flagged to Brayden with the fix (connect the GitHub repo in Vercel's dashboard). No Ohvara code/vault state changed. Pointer only, same reason as the prior entries below.
- [CC | 2026-08-15 — Restorix work this session, not Ohvara] Brayden asked to check Restorix's queue and run the next task — Prompt 429 (push `restorix-marketing` to GitHub now that Brayden had unblocked Vercel Authentication + created the empty repo). Restorix-scoped only, logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. No Ohvara code/vault state changed. Pointer only, same reason as the prior entry below.
- [CC | 2026-08-14 — Restorix work this session, not Ohvara] Brayden asked to check Restorix status then build Prompt 427 (marketing site). All of it is Restorix-scoped — logged in full to [[Restorix Memories]] and [[Restorix LIVE_STATE]], not duplicated here. No Ohvara code/vault state changed. Pointer only, since the Atlas logging gate fires per-vault-session regardless of which vertical the work touched.
- [CC | 2026-08-14 — queue check, nothing to run] Brayden said "run next task"; LIVE_STATE's "Next Up for CC" queue was already empty (last item, Prompt 423, is still waiting on Brayden's confirmation, not further build). Checked North Star's Current Focus per the empty-queue fallback — only unscoped Phase 1 backlog items there, not buildable without Eagle/Falcon scoping. Confirmed the two locally-modified files (`brain/training-flashcard-content.md`, `work/active/Prompt 313 Tonality Review.md`) are pure CRLF/LF line-ending noise, zero real content change — left uncommitted. Surfaced one CC-actionable candidate (zombie cron cleanup, Open Thread B) via AskUserQuestion; Brayden chose to queue something himself instead. No code/vault changes this session.
- [CC | 2026-08-14 — Prompt 426 shipped: Atlas deep-cleaned + split into Ohvara/Restorix verticals] Ran the full investigate-then-fix pass Brayden asked for rather than guessing at "bubbles." Root causes found: ~75 blank graph nodes from `[[Prompt N]]`/bare-3-digit/`[[BREAK]]` wikilinks in prose (individual prompt numbers routinely double-bracketed even though no per-prompt note was ever meant to exist) — unwrapped all of them to plain text across `LIVE_STATE.md`, `Memories.md`, both `session-logs/` files, and the Prompt History archive. Template-placeholder stubs under `archive/template-cruft/` were still graph-indexed — excluded the relevant subfolders via Obsidian's `userIgnoreFilters` (kept `session-flow-superseded/`/`work-hubs/` visible since real historical log entries still link into those). Deleted 11 genuinely orphaned single-use files (8 `brain/claude-chrome-*.md` browser-task scripts, 3 dead pre-pivot root files referencing the old SMB rep/setter pipeline) plus 2 stray empty root files and 2 stale git worktree dirs. `LIVE_STATE.md`'s CURRENT STATE section had re-bloated to ~137KB in 2 weeks (same pattern as the 2026-08-01 split) — archived 21 closed entries to [[LIVE_STATE Prompt History (2026-06 to 2026-07)]], kept 20 current ones, shrinking the file to ~94KB (delegated the mechanical extraction to a background agent, verified its byte accounting after). Created [[ohvara_legacy_setter_pipeline_dead]] (a memory note `LIVE_STATE.md` already referenced but that never existed) and [[Patterns]] (documented in CLAUDE.md's file list, never created, one real dangling link). Archived 3 stale `status: active` mobile-audit notes + screenshots from `work/active/` to `work/archive/2026/` (a month past done, no open follow-up). Added real inbound links for 7 orphaned-but-legitimate reference files instead of deleting them (setter transcripts, closer script rewrite, brand-asset prompts) — orphaned isn't automatically junk. **Restructure:** new `restorix/` folder mirrors `brain/`'s shape for Brayden's second business (behavioral-health AI infra) — `restorix/CLAUDE.md`, [[Restorix North Star]], [[Restorix LIVE_STATE]], [[Restorix Memories]], deliberately prefixed "Restorix" (not reusing `LIVE_STATE`/`North Star` names) to avoid ambiguous-wikilink collisions with Ohvara's own files. Prompts 427 (Restorix marketing site) and 428 (Restorix setter portal MVP) moved verbatim into Restorix's own queue. Root `CLAUDE.md` gained a routing preamble so a session reads the right vertical's state before doing anything else. Final whole-vault broken-wikilink sweep came back clean (0 real broken links, verified via script after each fix). Nothing pushed to `ohvara-dashboard` or `Scraper` this session — vault-only work. Full detail in [[2026-Q3]].
- [CC | 2026-08-05 — Prompt 425 shipped: Fulfillment nav trimmed + Overview redesigned, queue empty again] `1e6f313`. Same-day correction to Prompt 424 after Brayden saw the full Closer-parity nav live — dropped My Policies/Submissions/Commissions (none apply to Fulfillment's actual job), kept Live Call/Underwriting/Quoter/My Calls/Carrier Portals/Team/Training Center/Settings/Fulfillment Queue. Performance now skips straight to Leaderboard for this role (no Production tab). AgentOverview got a real redesign, not just hidden cards — new `FulfillmentOverview` with queue-shaped stats (Open in queue, Claimed by me, Today's callbacks team-wide, Completed by me today/month) derived from the existing `useFulfillmentQueue()` set, no new schema. Build+lint clean, dev server boots clean; still no live screenshot (same standing credentials wall). Queue empty again.
- [CC | 2026-08-05 — Prompt 424 shipped: Fulfillment gets full nav shell + header bell fix, LIVE_STATE queue now empty] `422364e`. Reversed Prompt 418's scoped-down Fulfillment nav per Brayden's live feedback — `Sidebar.jsx`'s `fulfillment` entry now mirrors `closer`'s full group set (Overview/Live Call/My Policies/My Calls/Quoter/Underwriting/Submissions/Carrier Portals/Performance/Team/Training Center/Commissions) with Fulfillment Queue folded into Account; `App.jsx` widened `allowedRoles` on the 12 corresponding `/agent/*` routes. Also found and fixed a real second gap along the way: `DashboardLayout.jsx`'s `HeaderBell()` had no branch for `fulfillment` at all (fell straight to null) — routed it through the existing `CloserNotificationBell`. RLS scoping from 418 untouched — nav/route visibility only. Build+lint clean, dev server boots with zero console errors; could not get the live authenticated screenshot Brayden asked for (no saved fulfillment-role credentials, same standing wall as recent prompts) — flagged in LIVE_STATE for his confirmation. Queue is now empty.
- [CC | 2026-08-01 — Prompts 402/403/404 shipped, queue empty except HOLD] Overview You/Everyone toggle + role-aware attention (404, `84a91ce`), Overview clock now reads account timezone not device local (403, `0d542e7`), Leaderboard gets Production's date-nav stepper for browsing past periods (402, `0ed6702`). LIVE_STATE's "Next Up for CC" queue is empty except Prompt 393 (Zoom Live Room), still on HOLD pending Brayden's real Zoom URL. Full detail, verification notes, and resume prompts in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 407 shipped: profile avatars everywhere] `d46e661` + migration 096 (applied live, Brayden-confirmed). New shared Avatar component replaces 13 bespoke single-initial spots app-wide with real photo-or-two-initial-colored-circle rendering; Profile page gets click-to-upload. Build+lint clean; screenshot verification blocked by the same login wall as Prompts 401/403/404. Full detail + resume prompt in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 409 shipped: persistent avatar-upload camera badge] `0364e42`. Small follow-up to 407 — added a small always-visible camera-icon badge on the Profile avatar circle so it reads as clickable at rest, not just on hover. Found already partially written (uncommitted) on disk when picking up the queue; verified lint/build clean and shipped it. Full detail in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 408 shipped: Add-carrier form opens near the button] `8d84047`. Moved the Carrier Portals "Add carrier" form from below the full card grid to directly under the header row, so it's visible without scrolling. Pure JSX reorder, no logic change. Build+lint clean; screenshot verification blocked by the same login wall (Claude-in-Chrome also not connected this session). Full detail in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 406 shipped: Production defaults to Monthly] `e0f8aab`. Performance → Production's period toggle now opens on Monthly instead of All Time, matching Leaderboard. One-arg change (`usePeriodPicker(today, 'monthly')`) plus updated stale comments that documented the old default. Build+lint clean (one pre-existing, unrelated lint finding confirmed via git stash); screenshot verification blocked by the same login wall. Full detail in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 405 shipped: "Default Overview to" moved Settings→Profile] `54bca96`. Correction to Prompt 404 — the You/Everyone default-scope toggle only means anything once "I'm also actively writing business" is ON, so it moved off Settings → Regional (standalone) onto Profile, in the same section as the monthly-goal field, sharing that exact reveal gate. Build+lint clean (0 findings in both files); screenshot verification blocked by the same login wall. Queue now empty except Prompt 393 (HOLD). Full detail in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 410 shipped: Compensation Grid bigger text/rows] `815262e`. Increased table/header font size, row padding, and tier column width in `CompensationGrid.jsx` per Brayden's explicit tradeoff (readable text over fitting all 16 tiers without horizontal scroll). Skipped the softer secondary ask (own vertical-scroll box for the ~150 rows) — flagged as not fully committed and needs a live look to combine cleanly with the existing sticky-left columns. Build+lint clean; same login-wall screenshot block as recent prompts. Queue now just Prompt 393 (Daily.co Live Room, active). Full detail in [[2026-Q3]].
- [CC | 2026-08-01 — Prompt 393 shipped: Live Room built on Daily.co, blocked on Brayden's signup] `5344634`. Full Team → Meetings Live Room built with Daily's React SDK (custom join/mute/camera/tiles UI, real avatars when camera's off, admin "notify team" button) — everything CC can build is built and pushed. Genuinely stopped short of "done": CC cannot create accounts (hard policy), so this needs Brayden to sign up free at daily.co, create one room, and paste the URL into Settings → Integrations. No API key needed anywhere. Zero further code changes once that URL exists. Queue is now fully empty. Full detail in [[2026-Q3]].
- [CC | 2026-08-02 — Prompts 412+411 shipped: real Balance (projected commission) + tab reorder/rename] `a941f39` + fix `2a1facd`, migration 098 applied live. `policies.estimated_commission` now computed via trigger from `commission_schedule`'s tier-70 rate (exact carrier+product match), backfilled live — verified one real match ($739.20 on a Mutual of Omaha policy) and confirmed Aflac/Baltimore Life/Chubb correctly land on null → rendered as "Pending," never $0. New `Balance.jsx` replaces the ComingSoon placeholder; Commissions tabs reordered (Balance first/default) and renamed from "Balance & Reserve." Caught + fixed same session: the migration's function drop+recreate initially lost the avatar fields Prompt 407 had added to `team_performance_policies()` — corrected live before wrap-up. Build+lint clean; screenshot verification hit the same login wall (prod URL also 404'd this session) — build- and live-DB-verified only. Full detail in [[2026-Q3]].
- [CC | 2026-08-02 — Prompt 413 shipped: My Policies You/Everyone fixes + admin agent filter] `07cc83a`. Restyled My Policies' bespoke toggle to the standard `Segmented` component; wired it into the "I'm also actively writing business" gate Overview/Performance already respect (hides for admin when off, forces Everyone); added an admin-only "Filter by agent" option; renamed Profile's "Default Overview to" → "Default view" and wired that same setting into My Policies' and Performance's initial scope too (was Overview-only before). Build+lint clean (1 pre-existing unrelated finding confirmed via git stash). Screenshot verification blocked — same login wall, plus the Vercel prod URL itself 404'd in-browser for a second session running, flagged for Brayden to check directly. Build-verified only. Full detail in [[2026-Q3]].
- [CC | 2026-08-02 — Prompt 414 shipped: Performance header rows unified] `e9e2153`. Production's Daily/Monthly/All Time toggle moved from right-aligned to left (same slot as Leaderboard's own period toggle, same size); Leaderboard's caption + date nav merged from two loosely-spaced rows into one tight stack. Removed the now-unused `PeriodPicker` wrapper component. Build+lint clean. **Vercel prod URL 404'd a third session running** — flagged directly to Brayden, this needs a look outside CC's tooling. Confirmed the app itself boots clean via local dev server (rules out this change breaking anything); actual authenticated screenshot still blocked by the standing login-credential wall. Full detail in [[2026-Q3]].
- [CC | 2026-08-03 — Prompt 415 shipped: Training Center page shell] `64d4d7e`, migration 099 applied live. `/agent/training` replaced `TrainingPlaceholder` with real Script/Videos/AI Voice Roleplay tabs (Commissions.jsx's exact `Segmented` pattern). Script tab is real: contentEditable + `execCommand` rich-text editor (no editor lib in the repo), admin-write/everyone-read, autosaves to `app_settings.training_script` ~1.5s after typing stops with a `beforeunload` guard against unsaved-on-close. Videos/Roleplay reuse the shared `ComingSoon` component, zero new logic. Build+lint clean (0 findings in any touched file). Screenshot verification blocked by the same standing login wall (Claude-in-Chrome not connected) — build+lint+local-dev-boot-verified only. Full detail in [[2026-Q3]].
- [CC | 2026-08-04 — Prompt 416 shipped: Training Center Script tab locked] `3d0eafb`, migration 100 applied live (`app_settings.training_script_locked`, default true — existing saved script flipped to locked automatically, content untouched at 5,897 chars). Script tab now read-only for every role including admin when locked (no textbox, no Save button); admin-only "Unlock to edit" control reopens the existing editor; re-locking is an explicit "Save & lock" action rather than auto-relock-on-autosave (would've yanked the editor away mid-keystroke). Build+lint clean (0 findings in touched files, 55 pre-existing repo-wide issues unchanged). Screenshot verification blocked by the same standing login wall — build+lint+migration-verified-live+local-dev-boot-verified only. Queue now just Prompt 417 (Training Center Videos tab) plus Prompt 393 (Daily.co, HOLD). Full detail in [[2026-Q3]].
- [CC | 2026-08-04 — Prompt 417 shipped: Training Center Videos tab (YouTube embeds)] `ad89088`, migration 101 applied live. New `training_videos` table (same admin-write/everyone-read RLS shape as `carriers`); admin gets an "Add video" form (validates the pasted URL parses to a real video ID via new `lib/youtube.js`) plus per-card delete/reorder; everyone gets a thumbnail-grid that swaps in a real `youtube.com/embed/{id}` iframe inline on click — no upload/re-hosting. Build+lint clean (0 findings, 55 pre-existing repo-wide issues unchanged). **Full CRUD path verified live**, not just build-verified: inserted a real test video row via SQL, confirmed it round-tripped, then deleted it so the table starts empty for Brayden. Screenshot verification blocked by the same standing login wall — build+lint+migration-verified-live+local-dev-boot-verified only. Queue is now fully empty except Prompt 393 (Daily.co, HOLD). Full detail in [[2026-Q3]].
- [CC | 2026-08-04 — Prompt 418 shipped: Fulfillment Team routing (new role + handoff queue)] `e69bdd0`, migrations 102+103 applied live. New `fulfillment` profile role + additive `policies` columns (`fulfillment_assigned`/`fulfillment_stage`/`assigned_fulfillment_id`/`scheduled_call_at`); two new ADDITIVE RLS policies scoped to `fulfillment_assigned = true` (existing agent/downline/admin policies untouched); a `fulfillment_complete_notify()` trigger (same shape as migration 090's pattern) pings the original agent when Fulfillment marks a handoff complete. **Both `apply_migration` calls were blocked outright by the auto-mode classifier this session** (schema changes to prod) — resolved via AskUserQuestion, Brayden confirmed both explicitly, same precedent as Prompt 407. Submissions' New Submission form gets an off-by-default "Send to Fulfillment Team" toggle (self-write flow untouched when off) that swaps Policy #/Effective date/Underwriting decision out for a Callback-time picker. New `/fulfillment` route + `FulfillmentQueue.jsx` page (fulfillment role + admin only) with Claim/Mark Complete actions, wired into Sidebar/App.jsx/ProtectedRoute/roleLabels/Users.jsx role dropdowns/notification bell icon maps. Build+lint clean (0 findings in new/touched files; pre-existing hits confirmed unrelated via git stash). **Flagged rather than guessed:** the prompt's own "cancels the client's old policy" framing has no linking column between the new and old policy rows in the 5 scoped build items — Mark Complete only flips the new policy's status, so actually cancelling the replaced policy isn't wired into this flow. Didn't invent a fix for that gap; surfaced it in LIVE_STATE for Brayden. Screenshot verification blocked by the same standing login wall — build+lint+migration-verified-live+local-dev-boot-verified only. Queue is now empty. Full detail in LIVE_STATE's Prompt 418 CURRENT STATE entry.
- [CC | 2026-08-04 — Prompt 419 shipped: Fulfillment intake form + soft scheduling default + client-facing copy] `9fe86b9`, migration 104 applied live. **Directly answers the gap flagged at the end of 418** — new `policy_fulfillment_details` table (1:1 via `policy_id`) adds `current_carrier` (plain text, Brayden: "all you gotta do is call up the carrier and say I want to cancel this policy") plus everything else Fulfillment needs to write the replacement policy (legal name/DOB/address/DL/beneficiary/draft day/bank routing+account). RLS on this table is TIGHTER than 418's broad queue-listing policy — only submitting agent, the specific claimant, or admin can read it, so an unclaimed item's PII stays invisible to the rest of the fulfillment role. New Submission form gained an 18-field intake section (shown only when the 418 toggle is on) + a "Read to the client" script hint using "Underwriting Team" instead of "Fulfillment Team" (Brayden doesn't want agents saying the internal name) + a soft (non-blocking) scheduling nudge for callback times more than a day out — muted suggestion caption, inline confirm-or-dismiss banner at submit only. Fulfillment Queue page gained an expandable per-row detail panel (claimed-by-me-or-admin gated) with DL/routing/account masked to last-4 behind a reveal toggle. **Caught + fixed same session:** the far-out confirm button's first draft re-called `submit()`, which would've read a stale closure value of `confirmFarOut` (React state isn't synchronous) and shown the same confirm forever — split into `submit()` (gate) + `doSubmit()` (the confirm button calls this directly). Build+lint clean; all 3 new RLS policies confirmed live via `execute_sql`. Screenshot verification blocked by the same standing login wall — build+lint+migration-verified-live+local-dev-boot-verified only. Queue is now empty. Full detail in LIVE_STATE's Prompt 419 CURRENT STATE entry.
- [CC | 2026-08-04 — Prompt 420 shipped: fixed the REAL cause of "419's fields missing in production" — a stale PWA service worker, not a rendering bug] `425c1c8`. Read `Submissions.jsx`'s entire `sendToFulfillment` conditional chain — every gate (required-fields list, callback-time swap, the 18-field intake block) is correctly wired to the one boolean; nothing was wrong with 419's shipped code. Brayden's screenshot was actually running 418's bundle (callback time visible, policy#/underwriting hidden) while missing 419's fields — only explainable if his browser was executing older JS than what Vercel had live. **Root cause:** the app is an installed PWA (`vite-plugin-pwa`, `generateSW` mode, `registerType: 'autoUpdate'`) whose generated service worker binds every navigation to its own precached `index.html` — an already-registered SW keeps serving whatever it last precached, across hard refreshes included, until it independently notices a new deploy (browser SW-byte checks are lazy, up to 24h). `skipWaiting`/`clientsClaim` were already on, but nothing forced an active update check or a reload once a fresher SW took control — this bug was guaranteed to recur on every future deploy for any tab/PWA session left open across it, unrelated to what any given prompt builds. **Fix:** `main.jsx` now registers via `virtual:pwa-register`, polls `updateSW()` every 30 min, reloads once (guarded) on `controllerchange`; `vite.config.js` disables the old auto-injected registration to avoid double-registering. Build+lint clean (0 new findings, same 55 pre-existing). **Verified live in a real browser** (not just build-checked): `vite preview` on port 4173, opened in the Browser pane, confirmed the SW registers cleanly via the new path, zero console errors, no stuck waiting/installing state. Claude-in-Chrome still not connected this session (same gap as every prompt since 407) — could not get the specific authenticated 18-fields-visible screenshot the prompt demanded. **Brayden still needs to confirm**: fully close (not refresh) the Submissions tab/PWA session and reopen, since the tab already showing the bug is running the OLD stuck SW that hasn't picked up this fix's update-check logic yet. Full detail in LIVE_STATE's Prompt 420 CURRENT STATE entry.
- [CC | 2026-08-04 — session close, Prompt 420 confirmed clean] Both repos verified clean/pushed after the Prompt 420 fix (`ohvara-dashboard` @ `425c1c8`, `obsidian-mind` @ `455e7ed`) — nothing further to log this session. Waiting on Brayden to reopen his Submissions tab fresh and confirm the 18 intake fields now render before this prompt can close.
- [CC | 2026-08-05 — Prompt 420 confirmed fixed by Brayden; Prompt 421 shipped: Fulfillment callback booking is now a date+slot picker] `6d2e7b2`. New session opened with Prompt 420 already resolved (Brayden's own screenshot confirmed all 18 intake fields render correctly post-fix). Picked up the next queued item: swapped the Fulfillment intake's raw `datetime-local` "Callback time" field for the same pick-a-date-then-pick-a-fixed-hourly-slot UI the existing Cancellation Calendar tab already uses. Extracted `SLOTS`/`slotToISO` out of `CancellationCalendar()` into new shared `src/lib/scheduling.js` so both booking flows share one implementation instead of drifting copies, per the prompt's explicit instruction. `form.scheduled_call_at` stays the single value of record (still an ISO string) — new `callbackDate`/`callbackSlot` picker state just recomputes it via `slotToISO()`, so `validate()`/`submit()`/`doSubmit()` and the existing far-out confirm-banner gate needed zero changes. Reused Cancellation Calendar's exact `GapNote` copy about no real capacity checking, per the prompt's instruction not to build that for one flow and not the other. Build+lint clean (0 new findings, same 55 pre-existing repo-wide — confirmed the one stray lint warning in this run is pre-existing in unrelated `NotificationToast.jsx`). Local dev server boots with zero console errors. **Could not get the live screenshot the prompt explicitly demanded** (login-gated form; Claude-in-Chrome checked twice this session, not connected either time) — ships build+lint+local-dev-boot-verified only, flagged to Brayden directly in LIVE_STATE for a confirming screenshot, same closing pattern as 420. Full detail in LIVE_STATE's Prompt 421 CURRENT STATE entry.
- [CC | 2026-08-05 — Prompt 422 shipped: avatar crop/zoom on upload + Remove photo] `c1788d8`. Closed the two gaps left in the avatar system since 407/409: photos uploaded as-is (off-center/stretched), and there was no way to clear one once set. Added `react-easy-crop` wired into a new circular crop/zoom modal; on confirm, a new `lib/imageCrop.js` renders the cropped rect into a fixed 512×512 canvas and uploads that Blob through the existing upload path unchanged. **Caught a real build failure, not just a warning:** the new library pushed the main bundle just over vite-plugin-pwa's hard 2 MiB precache ceiling, so `npm run build` failed outright. Fixed it properly with `React.lazy()`/`Suspense` code-splitting (the crop modal + library now load in their own ~27kB chunk only when opened) instead of just raising the size limit, since the library is only ever needed in this one rarely-triggered modal. New `useRemoveAvatar` hook lists the user's actual storage folder (not a fixed-extension guess) and deletes every file found before nulling `avatar_url` — existing delete RLS from migration 096 already covers it, no new policy needed. Build+lint clean (0 new findings, same 55 pre-existing); local dev server boots clean. **Could not get the 3 screenshots the prompt asked for** — login-gated page, Claude-in-Chrome still not connected (checked again). Ships build+lint+local-dev-boot-verified only, flagged to Brayden in LIVE_STATE, same closing pattern as 420/421. Full detail in LIVE_STATE's Prompt 422 CURRENT STATE entry.
- [CC | 2026-08-05 — Prompt 423 shipped: New Submission rewritten, no more Fulfillment toggle] `7d5c953`, migration 105 applied live (Brayden's explicit go-ahead, same AskUserQuestion precedent as 407/418). Brayden reversed his earlier "keep the toggle" call after seeing it live — removed the `Switch` entirely along with the three self-write-only fields it used to gate (Policy #, Effective date, Approved/In Underwriting); the form is now unconditionally the Fulfillment intake, `validate()`/`submit()`/`doSubmit()` no longer branch on anything. Fixed the duplicate-name bug: the separate "Full legal name" intake field is gone, `policy_fulfillment_details.full_legal_name` now derives from Client First/Last Name on submit. Callback time moved from mid-form to the very end, right before Log Submission. Beneficiaries went from flat `beneficiary_name`/`beneficiary_relationship` columns to a `beneficiaries jsonb` array (table had 0 rows live, checked via `execute_sql` before writing the migration — clean swap) — New Submission now supports multiple beneficiary rows with a Spouse/Child/Parent/Sibling/Other relationship dropdown, "+ Add another beneficiary," per-row Remove once there's more than one; Fulfillment Queue's detail panel updated to render the array. Build+lint clean (0 new findings, same 55 pre-existing); local dev server boots clean; migration verified live via `execute_sql`. **Could not get the 2 screenshots the prompt demanded** (full form top-to-bottom, two beneficiary rows) — same standing Claude-in-Chrome gap, checked again this session. Ships build+lint+local-dev-boot-verified+migration-verified-live only — **flagged for a closer-than-usual look**, this is the biggest structural rewrite this form's had (toggle removed, fields reordered, new multi-row UI). Queue is now empty. Full detail in LIVE_STATE's Prompt 423 CURRENT STATE entry.

